The two HTML files here are byte copies fetched from the Claude Design
project via DesignSync get_file on the dates in the README. They are
records, not vocabulary: nothing links to them, nothing mirrors them,
and the design bundle never includes them.
